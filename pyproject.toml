[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mgamdata"
version = "2.0.0.250524"
requires-python = ">= 3.10"
description = "mgam's data toolkit for a better world!"
readme = "README.md"
license = {file = "LICENSE"}
dependencies = [
  "openpyxl",
  "pandas",
  "scikit-image",
  "scikit-learn",
  "scipy",
  "future",
  "tensorboard",
  "orjson",
  "tqdm",
  "colorama",
  "ml_collections",
  "seaborn",
  "matplotlib",
  "matplotlib-inline",
  "jupyter",
  "colored",
]

[project.optional-dependencies]
dev = [
  "timm",
  "deepspeed",
  "h5py",
  "pylint-django",
  "HolisticTraceAnalysis",
  "pynvml",
]
deploy = [
  "onnx",
  "onnxruntime",
  "onnxruntime-gpu",
  "onnx_graphsurgeon",
  "polygraphy",
  "tensorrt",
]
medical_vision = [
  "monai",
  "opensdpc",
  "openslide-python",
  "openslide-bin",
  "SimpleITK",
  "pydicom",
  "pynrrd",
  "nibabel",
]
electrophysiology = [
  "lightning",
]


[project.scripts]
mmrun = "mgamdata.mm.run:main"
mmconc = "mgamdata.mm.summarize:main"
precrop = "mgamdata.process.PreCrop_3D:SemiSupervisedMhaCropper3D.main_entry"
itk_resample = "mgamdata.process.itk_resample:main"
itk_orient = "mgamdata.process.itk_orient:main"
split3d = "mgamdata.process.Split3D:main"
split2d = "mgamdata.process.Split2D:main"
itk_patch = "mgamdata.process.itk_patch:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["mgamdata"]
