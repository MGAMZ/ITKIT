[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mgamdata"
version = "2.2.0.250810"
requires-python = ">= 3.12"
description = "mgam's data toolkit for a better world!"
readme = "README.md"
license = {file = "LICENSE"}
dependencies = [
  "numpy>=2.3",
  "opencv-python>=4.11",
  "openpyxl",
  "pandas",
  "scikit-image",
  "scikit-learn",
  "scipy",
  "future",
  "tensorboard",
  "orjson",
  "tqdm",
  "colorama",
  "ml_collections",
  "seaborn",
  "matplotlib",
  "matplotlib-inline",
  "jupyter",
  "colored",
  "albumentations"
]

[project.optional-dependencies]
dev = [
  "h5py",
  "pylint-django",
  "HolisticTraceAnalysis",
  "pynvml",
]
nvidia = [
  "onnx",
  "onnxruntime",
  "onnxruntime-gpu",
  "onnx_graphsurgeon",
  "polygraphy",
  "tensorrt",
]
medical_vision = [
  "opensdpc",
  "openslide-python",
  "openslide-bin",
  "SimpleITK",
  "pydicom",
  "pynrrd",
  "nibabel",
]
pytorch = [
  "timm",
  "deepspeed",
]
mm = [
  "ftfy",
]

[project.scripts]
mmrun = "mgamdata.mm.run:main"
mmconc = "mgamdata.mm.summarize:main"
precrop = "mgamdata.process.PreCrop_3D:SemiSupervisedMhaCropper3D.main_entry"
itk_resample = "mgamdata.process.itk_resample:main"
itk_orient = "mgamdata.process.itk_orient:main"
split3d = "mgamdata.process.Split3D:main"
split2d = "mgamdata.process.Split2D:main"
itk_patch = "mgamdata.process.itk_patch:main"
itk_check = "mgamdata.process.itk_check:main"
itk_aug = "mgamdata.process.itk_aug:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["mgamdata"]
