# ITKIT Inference Server Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
flask>=2.0.0
flask-cors>=3.0.0
SimpleITK>=2.2.0
numpy

# ITKIT and deep learning (install based on your backend needs)
# For MMEngine backend:
# itkit[advanced]

# For ONNX backend:
# itkit[onnx]

# Or install both:
# itkit[advanced,onnx]
